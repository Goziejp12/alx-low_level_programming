#include <stdio.h>
#include <ctype.h>
/**
 * main - prints the alphabet in lower case
 * Return: (0)
 */

int main(0)
{
	char ch = a;

	while (ch <= z)
	{
	ch = tolower(ch);
	putchar(ch);
	ch++;
	}
	putchar("\n");
	return (0);

}
