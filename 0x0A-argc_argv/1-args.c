#include <stdio.h>
/**
 * main - a program that prints the nuber of arguments
 * passed into it
 * @argc: argument count variable
 * @argv: character variable
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
