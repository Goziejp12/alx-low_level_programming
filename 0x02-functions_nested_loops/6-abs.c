#include "main.h"
/**
 * _abs - prints absolute value of an integer
 * @n: holds the integer value
 * Return: 0
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
}
else
{
	return (0);
}
}
